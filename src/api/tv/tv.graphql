type Results {
  id: ID
  name: String
  original_name: String
  first_air_date: String
  original_language: String
  overview: String
  backdrop_path: String
  popularity: Float
  vote_average: Float
}

type TvDetail {
  id: ID
  name: String
  overview: String
  original_name: String
  popularity: Float
  vote_average: Float
}

type Query {
  airing_today(page: Int): [Results]
  popular(page: Int): [Results]
  top_rate(page: Int): [Results]
  tv_detail(id: Int): TvDetail
}
